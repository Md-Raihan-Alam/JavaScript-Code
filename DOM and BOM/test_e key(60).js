document.body.addEventListener('click',function(e){//keydown
console.log(e.altKey);//if the button is clicked than it will be true
console.log(e.shiftKey);//if the button is clicked than it will be true
console.log(e.ctrlKey);//if the button is clicked than it will be true
});