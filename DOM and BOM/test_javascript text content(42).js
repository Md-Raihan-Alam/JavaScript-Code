document.querySelector('button').addEventListener('click',replaceNow);
function replaceNow(){
document.querySelector('p').textContent="Replace by javascript";//replace the selected text with written one
}